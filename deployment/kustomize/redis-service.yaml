apiVersion: v1
kind: Service
metadata:
  name: litellm-redis
  labels:
    app: litellm-redis
spec:
  selector:
    app: litellm-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP
